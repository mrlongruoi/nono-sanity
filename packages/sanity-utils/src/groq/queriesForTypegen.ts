// queriesForTypegen.ts intentionally left empty.
// We previously used this file to re-declare GROQ queries for the typegen
// scanner, but it caused duplicate query-name errors because the same
// queries already exist in their original modules. Keeping this file empty
// prevents duplicates while still preserving the path if other tooling
// references it.

export const __typegen_queries_removed = true;
